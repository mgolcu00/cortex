# ============================================
# Confluence Q&A Chat - Environment Variables
# ============================================

# ============================================
# OpenAI Configuration
# ============================================
# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Chat model to use (default: gpt-4o)
# Options: gpt-4o, gpt-4-turbo, gpt-3.5-turbo, or your custom model
CHAT_MODEL=gpt-4o

# Embedding model (default: text-embedding-3-large)
# Options: text-embedding-3-large (3072 dim), text-embedding-3-small (1536 dim)
EMBEDDING_MODEL=text-embedding-3-large

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection URL (required)
# Format: postgresql+psycopg://user:password@host:port/database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/confluence_qa

# ============================================
# Confluence Configuration
# ============================================
# Confluence Cloud base URL (required)
# Example: https://yourcompany.atlassian.net/wiki
CONFLUENCE_BASE_URL=https://yourcompany.atlassian.net/wiki

# Confluence user email (required)
CONFLUENCE_EMAIL=your-email@company.com

# Confluence API token (required)
# Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
CONFLUENCE_API_TOKEN=your-confluence-api-token

# ============================================
# Sync Configuration
# ============================================
# Automatic sync interval in minutes (default: 60)
SYNC_INTERVAL_MINUTES=60

# Number of pages to process in one batch (default: 50)
SYNC_BATCH_SIZE=50

# ============================================
# Chunking Configuration (optional)
# ============================================
# Target chunk size in tokens (default: 750)
CHUNK_TARGET_TOKENS=750

# Minimum chunk size in tokens (default: 100)
CHUNK_MIN_TOKENS=100

# Maximum chunk size in tokens (default: 1000)
CHUNK_MAX_TOKENS=1000

# Overlap between chunks in tokens (default: 100)
CHUNK_OVERLAP_TOKENS=100

# ============================================
# Search Configuration (optional)
# ============================================
# Default number of chunks to retrieve (default: 30)
SEARCH_TOP_K=30

# Maximum number of pages to return (default: 12)
SEARCH_MAX_PAGES=12

# ============================================
# Logging Configuration
# ============================================
# Log level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
